<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{book.title}} - Book Details</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Roboto&display=swap" rel="stylesheet">
    <style>
        <!--styling for book-details pages-->
        body {
            background-color: #e5decc;
            color: #3e2723;
            padding: 50px;
            font-family: 'Roboto', sans-serif;
        }

        .book-container {
            max-width: 900px;
            margin: 0 auto;
            background-color: #fff8ec;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 6px 20px rgba(0,0,0,0.1);
            display: flex;
            flex-wrap: wrap;
            animation: fadeIn 0.7s ease;
        }

        /*defining animation steps */
        @keyframes fadeIn {
            from {opacity: 0;} /*starting with it being invisible*/
            to {opacity: 1;} /*fully visible*/
        }

        .book-cover {
            width: 250px;
            border-radius: 12px;
            margin-right: 40px;
            transition: transform 0.3s;
        }

        .book-cover:hover {
            transform: scale(1.05);
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        h3 {
            font-weight: 400;
            margin-bottom: 20px;
        }

        .genre-badge {
            display: inline-block;
            background-color: #d1bfa7;
            color: #3a2e1f;
            padding: 7px 15px;
            border-radius: 30px;
            font-weight: bold;
            margin-bottom: 20px;
            font-size: 0.95em;
        }

        .info-section {
            flex: 1;
            min-width: 300px;
        }

        .details p {
            margin: 8px 0;
            font-size: 1.1em;
        }

        .back-button {
            margin-top: 40px;
            text-align: center;
        }

        .back-button a {
            background-color: #d1bfa7;
            color: #3e2723;
            padding: 12px 25px;
            border-radius: 30px;
            font-weight: bold;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            transition: background-color 0.3s;
        }

        .back-button a:hover {
            background-color: #c8ae9e;
        }
    </style>
</head>
<body>

<div class="book-container">
    <img class="book-cover" src="{{book.thumbnailUrl}}" alt="Book Cover" onerror="this.onerror=null;
                                             this.src='https://covers.openlibrary.org/b/isbn/{{isbn}}-M.jpg';">

    <div class="info-section">
        <h1>{{book.title}}</h1>
        <h3>by {{book.author}}</h3>

        <div class="genre-badge">{{book.genre}}</div>

        <div class="details">
            <p><strong>Pages:</strong> {{book.pageCount}}</p>
            <p><strong>ISBN:</strong> {{book.isbn}}</p>
            <p><strong>Published on:</strong> {{book.publicationDate}}</p>
        </div>

        <p style="margin-top: 20px;">{{book.description}}</p>

        <div class="back-button">
            {{#email}}
                <a href="/home/{{email}}" class="ui button" style="background-color: #d1bfa7; color: #3a2e1f;">← Back to Home</a>
            {{/email}}
            {{^email}}
                <a href="/joinbooknest" class="ui button" style="background-color: #d1bfa7; color: #3a2e1f;">← Back to Home</a>
            {{/email}}
        </div>
    </div>
</div>

</body>
</html>
